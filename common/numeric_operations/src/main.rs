fn main() {
    // addition
    let sum = 5 + 10;

    // subtraction
    let difference = 95.5 - 4.3;

    // multiplication
    let product = 4 * 30;

    // division
    let quotient = 56.7 / 32.2;
    let truncated = -5 / 3; // Resuls in -1

    // remainder
    let remainder = 43 % 5;

    // boolean
    let t = true;

    let f: bool = false;

    // character
    let c = 'z';
    let z: char = 'Z' // with explicit type annotation
    let heart_eyed_cat = '🐱';
}
